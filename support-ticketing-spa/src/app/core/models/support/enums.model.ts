export enum Status {
    New = 'New', // Unassigned
    Open = 'Open', // Assigned & In Progress
    Closed = 'Closed'
}

export enum Priority {
    Low = 'Low',
    Medium = 'Medium',
    High = 'High'
}