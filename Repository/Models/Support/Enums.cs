namespace Repository.Models.Support
{
    public enum Status
    {
        New,
        Open,
        InProgress,
        Closed
    }

    public enum Priority
    {
        Low, 
        Medium,
        High
    }
}
